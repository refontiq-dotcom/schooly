# Dependencies
/node_modules

# Next.js
/.next/
/out/
next-env.d.ts

# Production
/build

# Env
.env
.env.local
.env*.local

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo

# Lockfiles — un seul gestionnaire de paquets pour ce projet (npm).
# package-lock.json est committé volontairement ; les autres sont ignorés
# pour éviter une résolution de dépendances divergente entre environnements.
bun.lock
bun.lockb
yarn.lock
pnpm-lock.yaml
